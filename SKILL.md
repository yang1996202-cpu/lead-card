---
name: lead-card
version: 0.1.2
description: 销售线索卡片生成器。输入公司名，输出一张结构化客户卡片，包含公司画像、关键人、行业洞察、破冰谈资建议。支持 --output 和 --my-product 参数。
allowed-tools:
  - Bash
  - Read
  - Write
  - WebFetch
  - Skill
---

# 销售线索卡片生成器

输入一个公司名，输出一张客户卡片。

## 使用方式

```
/lead-card {公司名} [--output <路径>] [--my-product <产品描述>]
```

**参数说明：**

| 参数 | 说明 | 示例 |
|------|------|------|
| `公司名` | 必填，目标公司名称 | `易点云` 或 `易点云有限公司` |
| `--output` | 可选，指定卡片保存路径，不指定时用默认路径 | `--output 03_归档/客户/` |
| `--my-product` | 可选，你的产品/业务，用于生成针对性谈资 | `--my-product IT设备租赁服务` |

**示例：**

```
# 基础用法
/lead-card 易点云

# 指定输出路径
/lead-card 易点云 --output 03_归档/客户/

# 结合你的业务生成针对性谈资
/lead-card 易点云 --my-product 企业培训服务
```

## 前置检查

### API Key 检查

执行前检查环境变量 `TAVILY_API_KEY` 是否存在：

```bash
[ -z "${TAVILY_API_KEY}" ] && echo "ERROR: TAVILY_API_KEY 未配置"
```

**如果未配置：**
- 提示用户："请先配置 TAVILY_API_KEY。参考：web-search-tavily skill 文档"
- 终止执行

### Rate Limit 说明

- Tavily API 有请求频率限制
- 建议：不要在短时间内大量调用（如 1 分钟内调用 10+ 次）
- 批量处理需求请在后续迭代中实现队列机制

## 输出

一张 Markdown 格式的客户卡片，包含：

1. **公司画像** - 行业、规模、业务、营收
2. **关键人信息** - 老板、核心高管
3. **公司动态** - 招聘活跃度、近期新闻
4. **行业洞察** - 行业现状、行业痛点
5. **破冰谈资建议** - 开场白方向、可能的痛点、联系优先级
6. **数据置信度** - 标注数据准确性

**卡片保存位置：**
- 默认：`01_项目/销售工具/客户卡片/{公司名}_{日期}.md`
- 自定义：`--output` 参数指定的路径

## 执行流程

### Step 0: 参数解析

- 检查公司名是否为空 → 空则报错终止
- 检查 `--output` 参数 → 设置保存路径
- 检查 `--my-product` 参数 → 记录用于谈资生成

### Step 1: 采集公司基本信息

**超时控制：** 使用 Bash 工具的 `timeout` 参数设置为 30000 毫秒（30秒）。如果搜索超时，工具会自动报错。

**搜索内容：**
- 公司全称、成立时间
- 人员规模、营收规模
- 法人/老板姓名
- 核心业务

**搜索命令：**
```bash
TAVILY_API_KEY="${TAVILY_API_KEY}" deno run --allow-env --allow-net=api.tavily.com ~/.claude/skills/web-search-tavily/scripts/search.ts "{公司名} 公司信息 法人 规模 营收" --results 5
```

**执行时设置：** `timeout: 30000`（Bash 工具参数）

**错误处理：**
- 超时（工具报错）→ 跳过此步骤，标注"公司基本信息：搜索超时"
- 失败（返回异常）→ 跳过此步骤，标注"公司基本信息：搜索失败"
- 成功 → 提取数据，计算置信度

### Step 2: 采集官网/产品信息

**超时控制：** 使用 Bash 工具的 `timeout` 参数设置为 30000 毫秒。

**搜索内容：**
- 主营产品/服务
- 目标客户群体
- 差异化卖点

**搜索命令：**
```bash
TAVILY_API_KEY="${TAVILY_API_KEY}" deno run --allow-env --allow-net=api.tavily.com ~/.claude/skills/web-search-tavily/scripts/search.ts "{公司名} 官网 产品 业务" --results 5
```

**执行时设置：** `timeout: 30000`

**错误处理：**
- 超时 → 跳过，标注"官网信息：搜索超时"
- 失败 → 跳过，标注"官网信息：搜索失败"
- 无官网 → 标注"官网信息：暂无数据"

### Step 3: 采集招聘活跃度

**超时控制：** 使用 Bash 工具的 `timeout` 参数设置为 30000 毫秒。

**搜索内容：**
- 在招岗位类型
- 招聘数量
- 判断公司状态（扩张/稳定/收缩）

**搜索命令：**
```bash
TAVILY_API_KEY="${TAVILY_API_KEY}" deno run --allow-env --allow-net=api.tavily.com ~/.claude/skills/web-search-tavily/scripts/search.ts "{公司名} 招聘 岗位" --results 5
```

**执行时设置：** `timeout: 30000`

**错误处理：**
- 超时 → 跳过，标注"招聘信息：搜索超时"
- 失败 → 跳过，标注"招聘信息：搜索失败"
- 无招聘 → 标注"招聘信息：暂无数据"

### Step 4: 采集新闻/行业动态

**超时控制：** 使用 Bash 工具的 `timeout` 参数设置为 30000 毫秒。

**搜索内容：**
- 公司动态
- 行业趋势
- 潜在机会/风险

**搜索命令：**
```bash
TAVILY_API_KEY="${TAVILY_API_KEY}" deno run --allow-env --allow-net=api.tavily.com ~/.claude/skills/web-search-tavily/scripts/search.ts "{公司名} 新闻 动态 {行业名} 行业趋势" --results 5
```

**执行时设置：** `timeout: 30000`

**错误处理：**
- 超时 → 跳过，标注"新闻动态：搜索超时"
- 失败 → 跳过，标注"新闻动态：搜索失败"
- 无新闻 → 标注"新闻动态：暂无数据"

### Step 5: 整合生成客户卡片

**置信度计算：**

| 情况 | 置信度 |
|------|--------|
| 搜索结果公司名完全匹配 | 高 |
| 搜索结果公司名部分匹配（含关键词） | 中 |
| 搜索结果公司名不匹配或模糊 | 低 |

**自检步骤：**

生成卡片后，执行自检：
1. 公司名是否与输入一致 → 不一致则标注"低置信度"
2. 关键人姓名是否合理（非乱码、非明显错误） → 异常则标注"数据可能不准确"
3. 营收/规模数据是否在合理范围 → 异常则标注"数据可能不准确"

**谈资生成逻辑：**

如果提供了 `--my-product`：
- 结合用户产品 + 目标公司痛点 → 生成针对性开场白
- 例如：用户卖企业培训 + 目标公司在招大量新人 → 建议"新人培训效率"话题

如果没有 `--my-product`：
- 生成通用开场白
- 标注"建议结合你的业务调整开场白"

### Step 6: 保存卡片

**默认路径：** `01_项目/销售工具/客户卡片/{公司名}_{日期}.md`

**自定义路径：** 使用 `--output` 参数

**路径不存在：** 自动创建目录

**文件名冲突：** 覆盖旧文件（用户可自行备份）

## 输出模板

```markdown
# 客户卡片：{公司名}

> 数据置信度：{高/中/低} — {理由}
> {如有异常：⚠️ 数据可能不准确，请核实后使用}

## 一、公司画像

| 维度 | 信息 |
|------|------|
| 行业 | {行业类型 或 暂无数据} |
| 主营业务 | {一句话概括 或 暂无数据} |
| 人员规模 | {人数 或 暂无数据} |
| 成立年份 | {年份 或 暂无数据} |
| 营收规模 | {如有公开数据 或 暂无数据} |
| 融资/上市 | {阶段 或 暂无数据} |

{如有搜索失败：⚠️ 公司基本信息：搜索超时/失败}

## 二、关键人信息

| 角色 | 姓名 | 背景（如有） |
|------|------|-------------|
| 法人/老板 | {姓名 或 暂无数据} | {背景 或 暂无数据} |
| 核心高管 | {姓名 或 暂无数据} | {背景 或 暂无数据} |

## 三、公司动态

### 招聘活跃度
- 在招岗位：{岗位列表 或 暂无数据}
- 招聘数量：{数量 或 暂无数据}
- 判断：{扩张中/稳定/收缩 或 暂无数据}

{如有搜索失败：⚠️ 招聘信息：搜索超时/失败}

### 近期新闻
- {新闻1 或 暂无数据}
- {新闻2 或 暂无数据}
- {新闻3 或 暂无数据}

{如有搜索失败：⚠️ 新闻动态：搜索超时/失败}

## 四、行业洞察

### 行业现状
{行业整体情况 或 暂无数据}

### 行业痛点
{该行业常见的痛点 或 暂无数据}

## 五、破冰谈资建议

{如有 --my-product：基于你的业务生成}
{如无 --my-product：通用建议 + 提示结合业务调整}

### 开场白方向
{基于以上信息，建议的破冰角度}

### 可能的痛点
{推测的客户可能面临的问题}

### 联系优先级
{高/中/低，以及理由}

---
生成时间：{时间}
数据来源：企查查、官网、招聘网站、新闻
用户产品：{如有 --my-product 则显示，否则显示"未指定"}
```

## 测试场景

以下场景需手动测试验证：

| 场景 | 输入 | 预期输出 |
|------|------|----------|
| 正常流程 | `/lead-card 易点云` | 生成完整卡片，置信度标注 |
| 公司名空输入 | `/lead-card` | 报错："公司名不能为空" |
| 公司名特殊字符 | `/lead-card 北京-abc公司` | 正确处理，生成卡片 |
| `--output` 参数 | `/lead-card 易点云 --output 03_归档/` | 保存到指定路径 |
| `--my-product` 参数 | `/lead-card 易点云 --my-product 企业培训` | 谈资关联企业培训 |
| API Key 不存在 | 未配置 TAVILY_API_KEY | 报错："请先配置 TAVILY_API_KEY" |
| 搜索超时 | 模拟超时环境 | 跳过步骤，标注"搜索超时" |
| 公司名匹配低置信度 | `/lead-card 华为`（同名公司多） | 置信度标注"低"，提示核实 |
| 无官网 | 小公司无官网 | 标注"官网信息：暂无数据" |
| 无招聘 | 公司无招聘信息 | 标注"招聘信息：暂无数据" |
| 无新闻 | 公司无近期新闻 | 标注"新闻动态：暂无数据" |
| 数据不完整 | 多步搜索失败 | 标注缺失项 |
| 自检发现异常 | 关键人姓名明显错误 | 标注"数据可能不准确" |
| 输出路径不存在 | `--output 新目录/` | 自动创建目录 |
| 文件名冲突 | 同名文件已存在 | 覆盖旧文件 |

## 数据源

| 数据源 | 获取信息 | 特点 |
|--------|----------|------|
| Tavily 搜索 | 企查查/天眼查页面 | 免费，但需配置 API Key |
| 官网 | 产品、业务方向 | 小公司可能无官网 |
| 招聘网站 | 公司活跃度 | 无招聘时不代表不活跃 |
| 新闻/舆情 | 行业动态 | 信息时效性需关注 |

## 后续迭代方向

1. **企查查 API 接入**：付费但更准确的数据源
2. **并行搜索优化**：4 步并行执行，总耗时降到 3 秒
3. **批量处理**：支持输入多个公司名，批量生成卡片
4. **CRM 集成**：读取 CRM 中的历史记录
5. **缓存机制**：同公司名 24 小时内复用缓存

## 设计理念

这个工具的核心价值不是"数据"——企查查、天眼查都有数据。

核心价值是"整合+解读+针对性建议"：
- 把分散在多个平台的信息整合到一张卡片
- 用销售视角解读这些信息意味着什么
- 结合你的业务生成可执行的谈资建议

这是 6 年 B 端销售经验的方法论沉淀。

## 更新日志

**v0.1.1 (2026-04-12)**
- 加 `--output` 参数支持自定义保存路径
- 加 `--my-product` 参数生成针对性谈资
- 加 API Key 检查和提示
- 加每步超时机制（5秒）
- 加错误处理：失败时跳过并标注
- 加置信度标注
- 加自检步骤验证数据
- 空白字段显示"暂无数据"
- 加 Rate Limit 说明
- 加测试场景列表

**v0.1.0 (2026-04-10)**
- 初始版本，基础搜索和卡片生成